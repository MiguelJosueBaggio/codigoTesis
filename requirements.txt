# Núcleo estadístico (pipeline/analysis_core.py) — change anova-tukey-core
pandas>=2.0
statsmodels>=0.14
scipy>=1.10
numpy>=1.24

# Testing
pytest>=8.0

# Cobertura (change session-engine, C-12, task 8.2 -- verificar 100% del
# motor de sesiones con pytest-coverage)
pytest-cov>=5.0

# Andamiaje del pipeline (validacion, visualizacion, persistencia) — change foundation-setup
# Pin >=1.0,<2.0 confirmado por el change validation-engine (C-04, Decision 4):
# la linea 0.18.x exige numpy<2 (incompatible con pandas 3 ya en uso); la 1.x
# conserva unexpected_index_list con result_format=COMPLETE (verificado en 1.18.2).
great_expectations>=1.0,<2.0
matplotlib>=3.8

# Pin acotado (change persistence-audit-module, C-06, Decision 1 del design,
# checkpoint resuelto 2026-07-03): mismo estilo de rango que
# great_expectations arriba. Instalabilidad verificada en el venv del
# proyecto: SQLAlchemy 2.0.51 / Alembic 1.18.5.
SQLAlchemy>=2.0,<3.0
Alembic>=1.13,<2.0

# Meta-schema del diccionario de variables (pipeline/data_dictionary.py) — change data-dictionary-schema
jsonschema>=4.20

# Lectura de Excel (pipeline/ingestion.py) — change ingestion-module
openpyxl>=3.1

# Config YAML re-ejecutable (pipeline/analysis.py) — change statistical-analysis-module
# (C-07, OQ1 resuelta): verificado `import yaml` OK en el venv del proyecto
# (PyYAML 6.0.3) antes de fijar el pin (mismo protocolo que great_expectations/
# SQLAlchemy arriba).
PyYAML>=6.0,<7.0

# Fuzzy matching determinista para sugerencias de estandarizacion lexica
# (pipeline/ai_support.py) — change ai-support-standardization (C-09, D-2 del
# design). NO usa LLM. Instalabilidad verificada en el venv del proyecto:
# rapidfuzz 3.14.5 (mismo protocolo que great_expectations/SQLAlchemy arriba).
rapidfuzz>=3.0,<4.0
